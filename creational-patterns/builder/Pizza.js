class Pizza {
    constructor() {
        this.form
        this.topping
        this.sause
    }
    setForm(form) {
        this.form = form
    }
    setTopping(topping) {
        this.topping = topping
    }
    setSause(sause) {
        this.sause = sause
    }
}

export default Pizza