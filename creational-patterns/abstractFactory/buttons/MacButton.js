import AbstractButton from './AbstractButton';
class MacButton extends AbstractButton {
    constructor() {
        super()
    }

    render() {
        console.log('render Mac type button')
    }
}

export default MacButton